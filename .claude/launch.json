{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "allepizza-dev",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import os,http.server,socketserver; PORT=int(os.environ.get('PORT', 8097)); socketserver.TCPServer(('', PORT), http.server.SimpleHTTPRequestHandler).serve_forever()"],
      "port": 8097,
      "autoPort": true
    }
  ]
}
